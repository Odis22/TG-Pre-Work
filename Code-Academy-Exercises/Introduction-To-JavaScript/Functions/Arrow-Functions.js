const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};

/* is a shorter way to write function by using an 
=> thats points to the function body thats in the 
bracket
*/
