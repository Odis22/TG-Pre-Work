function monitorCount(rows, columns) {
 return rows * columns
}


const numOfMonitors = monitorCount(5,4);
console.log(numOfMonitors);

/* 
the return statement passes back information from a function call using the the return keywork
*/