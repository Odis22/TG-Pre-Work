// The loop below loops from 0 to 3. Edit it to loop backwards from 3 to 0
for (let counter = 3; counter >= 0; counter--){
  console.log(counter);
}

/* to run a loop backward, set it to the highest desired value and set the stopping condition for when the iterator variable is less than the desired amount
*/