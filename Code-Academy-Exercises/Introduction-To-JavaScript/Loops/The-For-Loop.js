// Write your code below
for (let counter = 5; counter < 11; counter++) {
  console.log(counter);
} 

/*for loop has iterator variable that is initialized, checked against the stoppping condition, and assigned a new value on each loop iteration.
*/
